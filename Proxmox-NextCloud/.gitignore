# Don't commit sensitive or site-specific stuff!
config.yaml
ssh_keys/*

# Personal vault-id label/filename — only the .example is committed.
ansible.cfg

# A vault file ever decrypted in place by mistake.
*.vault.yaml.decrypted
