# Don't commit runtime files
.proxmox_credentials.yaml
config.yaml

# Repo-local Ansible toolchain, built by Shared/Ansible/bootstrap.sh.
.venv/
.ansible/

# This repo is public. An Ansible inventory carries real node names / IPs;
# only the matching .example is committed. Bare pattern -> matches
# inventory/hosts.ini at every level of the tree.
hosts.ini

# Things are aren't yet for the eyes of GitHub
Proxmox-AlphaCharlie/
